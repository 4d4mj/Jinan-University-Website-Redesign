export interface Profile {
    id: string;
    major: string;
    name: string;
    campus: string;
    semester: string[];
  }

export interface Courses {
    code: string;
    name: string;
    credits: string;
    type: string;
    grade: string;
}

export interface Transcript{

}
